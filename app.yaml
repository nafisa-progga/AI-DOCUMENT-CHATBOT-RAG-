runtime: python39
entrypoint: streamlit run app.py --server.port 8080 --server.address 0.0.0.0

instance_class: F2

env_variables:
  GOOGLE_API_KEY: "YOUR_KEY_HERE" # Or use Secret Manager for better security